#include <iostream>
#include <string>
#include "Challenges.h"
using namespace std;

Challenges :: Challenges()
{
    //constructor
}

Void setCompletedLevel(int, int, bool)
{
    //takes position on matrix and bool as to whether the user completed the
    //level, set that position to the bool value
}

Bool checkLevels()
{
    //checks which levels are finished, so we can mark them as completed
    //in the challenges menu so user picks only between the levels not yet done
}
Bool finishedChallenges()
{
    // checks if all the challenges are finished 
    // so we only give the user the option to quit or move on to the next tier
}
